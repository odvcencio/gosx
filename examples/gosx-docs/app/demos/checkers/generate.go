package checkers

// Regenerate the browser-independent visual fallback from the same typed
// Scene3D composition used by the live demo.
//go:generate go run ./previewgen -out ../../../public/checkers-native-preview.png
