apiVersion: v1
kind: Secret
metadata:
  name: gosx-docs
  namespace: draco-quest
type: Opaque
stringData:
  session-secret: 8d6672f457f91af4ac9b1d5060b9f00f3f440933af4aa5b905f26e97da28f17a
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gosx-docs
  namespace: draco-quest
  labels:
    app: gosx-docs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gosx-docs
  template:
    metadata:
      labels:
        app: gosx-docs
    spec:
      containers:
        - name: gosx-docs
          image: harbor.draco.quest/library/gosx-docs:20260327-215616
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: PORT
              value: "8080"
            - name: PUBLIC_URL
              value: https://gosx.m31labs.dev
            - name: SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: gosx-docs
                  key: session-secret
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: gosx-docs
  namespace: draco-quest
spec:
  selector:
    app: gosx-docs
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gosx-docs
  namespace: draco-quest
  annotations:
    cert-manager.io/cluster-issuer: cloudflare-issuer
    traefik.ingress.kubernetes.io/router.priority: "20"
spec:
  ingressClassName: traefik
  rules:
    - host: gosx.m31labs.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gosx-docs
                port:
                  name: http
  tls:
    - hosts:
        - gosx.m31labs.dev
      secretName: gosx-docs-tls
