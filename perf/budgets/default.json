{
  "defaultProfile": "basic-island",
  "profiles": {
    "static": {
      "description": "Server-rendered route with no external client runtime.",
      "assertions": [
        "js_total_kb == 0",
        "lcp <= 1500",
        "long_tasks == 0",
        "tbt <= 0"
      ]
    },
    "basic-island": {
      "description": "Server-first route with navigation, forms, or small islands.",
      "assertions": [
        "js_total_kb <= 120",
        "lcp <= 2500",
        "long_tasks <= 2",
        "tbt <= 150"
      ]
    },
    "wasm-island": {
      "description": "Route that intentionally loads the GoSX WASM island runtime.",
      "assertions": [
        "js_total_kb <= 180",
        "lcp <= 2500",
        "long_tasks <= 3",
        "tbt <= 200"
      ]
    },
    "scene3d": {
      "description": "Interactive Scene3D route profiled on a throttled mobile-class browser.",
      "assertions": [
        "lcp <= 3000",
        "long_tasks <= 3",
        "tbt <= 250",
        "scene_p95 <= 33",
        "scene_p99 <= 50",
        "presented_p95? <= 33",
        "gpu_total_p95? <= 20"
      ]
    },
    "docs-scene3d-ci": {
      "description": "Spot-shadow runtime adds exactly 2147 raw bytes across shared Scene3D (1371), WebGL (361), and WebGPU (415). Measured docs network 1550.4072265625KiB vs previous 1548.310546875KiB; new 1551KiB ceiling rounds up this observed payload. JS 1316.22KiB, LCP 1608ms, TBT 1075ms pass unchanged. Software rasterization, not hardware certification; fresh remote CI pending.",
      "assertions": [
        "js_total_kb <= 1550",
        "network_kb <= 1551",
        "lcp <= 18000",
        "tbt <= 15000"
      ]
    },
    "water-ci": {
      "description": "Parent remote CI 33199896900 (ac1752d5) failed water network at 1690.42KiB > 1686, while timings and functional tests passed. New spot chunks add 2147 raw bytes maximum when both backends load; conservative projection 1690.42 + 2147/1024 = 1692.5166796875KiB, rounded 1693. This is NOT measured new remote CI. Local WebGPU water 1460.765625KiB, LCP 1228ms, TBT 1083ms pass. Timing gates unchanged; no hardware certification.",
      "assertions": [
        "network_kb <= 1693",
        "lcp <= 2000",
        "tbt <= 18000"
      ]
    }
  },
  "routes": [
    {
      "url": "/",
      "profile": "basic-island"
    },
    {
      "url": "/docs/getting-started",
      "profile": "docs-scene3d-ci"
    },
    {
      "url": "/demos/water",
      "profile": "water-ci"
    }
  ]
}
