/dashboard
build/
.canopy/
/docs/
/gosx
node_modules/

# Local dev data (SQLite, uploads) — never commit
data/

# Specs and plans — never commit
.claude/
docs/blog-outlines/
docs/specs/
docs/superpowers/

# Local dev workspace overlay (links sibling prism/selena checkouts) — never commit
go.work
go.work.sum
