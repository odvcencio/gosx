// A generated bootstrap bundle. The walk skips it, so the marker below never
// reaches the checker. The meta test asserts that no result names this file.
//	gosx:claim has target.go `func Gamma` case-skipped-bundle
export const generated = true;
